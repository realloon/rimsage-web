---
import GitHubIcon from './icons/GitHubIcon.astro'

const navLinks = [
  { href: '#features', label: 'Features' },
  { href: '#tools', label: 'Tools' },
  { href: '#connect', label: 'Connect' },
  { href: '#self', label: 'Self-Hosted' },
]
---

<header>
  <a href="/" style="font-weight: bold;">RimSage</a>

  <nav>
    {navLinks.map(link => <a href={link.href}>{link.label}</a>)}
  </nav>

  <a
    class="repo-link"
    href="https://github.com/realloon/RimSage"
    target="_blank"
    rel="noreferrer"
    aria-label="repository"
    title="GitHub"
  >
    <GitHubIcon />
  </a>
</header>

<style>
  header {
    z-index: 1;
    position: sticky;
    top: 0;

    display: flex;
    align-items: center;

    background-color: var(--panel);
    backdrop-filter: blur(8px);
    border-block-end: 1px solid var(--line);
    padding: 8px var(--page-gap);
  }

  a:hover {
    text-decoration: unset;
  }

  nav {
    display: flex;
    margin-left: 16px;

    @media (max-width: 900px) {
      display: none;
    }

    a {
      color: var(--muted);
      font-size: 14px;
      padding: var(--space-1) var(--space-2);
      border-radius: 999px;
      transition: 0.2s;

      &:hover {
        color: var(--ink);
        background-color: light-dark(#0da2e71a, #38bdf833);
      }
    }
  }

  .repo-link {
    border-radius: 50%;
    margin-left: auto;

    svg {
      vertical-align: top;
      width: 32px;
      height: 32px;
    }
  }
</style>
